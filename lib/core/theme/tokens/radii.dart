class AppRadii {
  AppRadii._();

  /// Small radius for compact UI elements (checkboxes, chips).
  static const double radius4 = 4.0;

  /// Medium radius for general containers/cards.
  static const double radius8 = 8.0;

  /// Default radius for tappable surfaces (tiles, buttons).
  static const double radius12 = 12.0;

  /// Large radius for sheets/modals.
  static const double radius24 = 24.0;

  /// “Pill” radius used to force fully-rounded surfaces.
  static const double radiusPill = 999.0;
}
