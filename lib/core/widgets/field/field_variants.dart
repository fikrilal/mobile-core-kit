enum FieldVariant { primary, secondary, outline, filled }

enum FieldSize { small, medium, large }

enum FieldState { enabled, disabled, error, success, warning }

enum FieldType { text, email, password, number, phone, url, multiline, search }

enum LabelPosition { floating, above }
