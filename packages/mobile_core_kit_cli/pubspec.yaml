name: mobile_core_kit_cli
description: Repository-local tooling CLI for mobile-core-kit.
version: 0.0.1
publish_to: "none"

environment:
  sdk: ^3.10.3

dependencies:
  args: ^2.7.0
  path: ^1.9.1

dev_dependencies:
  test: ^1.30.0

executables:
  mobilekit: mobilekit
