# Contracts

Docs intended to be shared across teams (backend/API semantics, auth guarantees, idempotency expectations).

Subfolders:

- `docs/contracts/auth/` — authentication/refresh/retry contracts.

